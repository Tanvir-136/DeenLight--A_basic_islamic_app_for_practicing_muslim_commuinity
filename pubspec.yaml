name: deenlight
description: "A basic Islamic practice app"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.8.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^0.13.5
  intl: any
  shared_preferences: ^2.2.2
  connectivity_plus: ^5.0.1
  provider: ^6.0.5
  cupertino_icons: ^1.0.8
  geolocator: ^11.0.1
  geocoding: ^2.0.5
  permission_handler: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  generate: true
  
  # assets:
  #   - assets/translations/
  
localizations:
  enabled: true